name: Build P6YTOK IPA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "IPA build is temporarily paused while the final source type errors are repaired."
